<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><i class="far fa-<%= task.completed ? 'check-square' : 'square' %>"></i> <%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to 'Add a new task', new_task_path %>
